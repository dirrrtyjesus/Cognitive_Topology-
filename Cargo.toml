[package]
name = "cognitive_topology"
version = "0.1.0"
edition = "2021"
description = "Geometry as the generative kernel of cognition - manifold computation"
license = "MIT"

[dependencies]
nalgebra = "0.33"
rand = "0.8"
thiserror = "1.0"
ndarray = "0.15"

[dev-dependencies]
criterion = "0.5"
